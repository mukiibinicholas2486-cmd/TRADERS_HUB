services:
  - type: web
    name: traders-hub
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DERIV_CLIENT_ID
        sync: false
      - key: DERIV_REDIRECT_URI
        sync: false
      - key: SESSION_SECRET
        generateValue: true
